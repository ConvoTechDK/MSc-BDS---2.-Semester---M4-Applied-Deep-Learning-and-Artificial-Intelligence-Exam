# Requirements for M4 Exam Portfolio
# Install: pip install -r requirements.txt
# GPU jobs (fine-tuning, QLoRA) are run on HPC via SLURM scripts.

# Core ML
torch>=2.1.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# HuggingFace ecosystem
transformers>=4.40.0
datasets>=2.18.0
sentence-transformers>=2.6.0
evaluate>=0.4.0
accelerate>=0.29.0
peft>=0.10.0
bitsandbytes>=0.43.0

# OpenAI + Agents
openai>=1.20.0
crewai>=0.28.0
langchain-openai>=0.1.0

# Visualization / notebooks
matplotlib>=3.7.0
seaborn>=0.12.0
jupyter>=1.0.0

# Data storage
pyarrow>=14.0.0
